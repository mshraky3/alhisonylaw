{
  "name": "alhisonylaw",
  "version": "1.0.0",
  "description": "React Vite + Express full-stack app",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd Backend && npm run dev",
    "dev:frontend": "cd React && npm run dev",
    "install:all": "npm install && cd Backend && npm install && cd ../React && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

